defaultNamespace: cattle-elemental-system
name: elemental-crds
helm:   
  # chart: oci://registry.suse.com/rancher/elemental-operator-chart
  repo: oci://registry.suse.com/rancher/elemental-operator-chart     
  releaseName: elemental-operator-crds
  atomic: true
  waitForJobs: true
  timeoutSeconds: 300
  takeOwnership: truerolloutStrategy:
  maxUnavailable: 15%
  maxNew: 50labels:
  elemental: crds
